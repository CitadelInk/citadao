{
  "name": "citadao-devenv",
  "version": "0.1.0",
  "description": "Citadao env",
  "repository": "https://github.com/nickreynolds/citadao.git",
  "contributors": [
    "Andrew Spiering",
    "Nick Reynolds"
  ],
  "license": "MIT",
  "devDependencies": {},
  "scripts": {
    "postinstall": "local_modules install --force --dir src",
    "devsetup": "yarn install && local_modules link --force --dir src && ( cd src/app-contracts && yarn install ) && ( cd src/app-frontend && yarn install ) && ( cd src/app-apiserver && yarn install )",
    "testrpc": "( cd src/app-contracts && yarn testrpc )"
  },
  "dependencies": {
    "local_modules": "^1.0.8"
  },
  "main": "index.js"
}